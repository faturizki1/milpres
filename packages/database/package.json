{
  "name": "@milpers/database",
  "version": "0.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "migrate": "prisma migrate dev --name init",
    "migrate:apply": "prisma migrate deploy",
    "prisma:generate": "prisma generate",
    "db:seed": "prisma db seed"
  },
  "devDependencies": {
    "prisma": "^5.12.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.12.0"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  }
}
